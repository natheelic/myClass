# อธิบายการทำงานของ `visualize.py` ทีละบรรทัด

ไฟล์ `visualize.py` ทำหน้าที่แสดงผลภาพกราฟและทางเดินที่ประมวลผลออกมาได้ด้วยเครื่องมือวาดรูปภาพ:

---

| บรรทัดที่ | โค้ด | คำอธิบาย |
| :---: | :--- | :--- |
| **1-8** | `""" ... """` | คอมเมนต์อธิบายวัตถุประสงค์การใช้กราฟิกและ subplot ในการเทียบอัลกอริทึม |
| **10** | `import matplotlib` | นำเข้าไลบรารีกราฟิกหลักเพื่อตั้งค่าระบบพล็อตหลัก |
| **11** | `import matplotlib.pyplot as plt` | นำเข้าระบบย่อยสำหรับสร้างหน้าจอการวางแผนภาพกราฟิก |
| **12** | `import networkx as nx` | นำเข้าโมดูล networkx เพื่อสั่งวาดโครงสร้างแบบแผนภูมิกราฟ |
| **13** | `from matplotlib import font_manager` | ดึงระบบตรวจสอบข้อมูลฟอนต์อักษรในเครื่องคอมพิวเตอร์ |
| **15** | `from graph_builder import get_positions` | นำฟังก์ชันพิกัดจากไฟล์สร้างกราฟมาใช้ร่วมกัน |
| **18** | `def _setup_thai_font():` | ประกาศฟังก์ชันตั้งค่าฟอนต์ภาษาไทยเพื่อไม่ให้โปรแกรมแสดงกล่องข้อความเต้าหู้ |
| **20-21**| `candidates = [ ... ]` | ลิสต์รายชื่อฟอนต์ไทยยอดนิยมในเครื่อง Mac/Windows/Linux |
| **22** | `available = {f.name for f in font_manager.fontManager.ttflist}` | ค้นหาและทำลิสต์ชื่อฟอนต์ทั้งหมดที่มีในเครื่องคอมพิวเตอร์นี้ |
| **23** | `for name in candidates:` | วนลูปตรวจสอบฟอนต์ที่แนะจำทีละชื่อ |
| **24** | `if name in available:` | ถ้าชื่อฟอนต์ที่เลือกมีอยู่ในเครื่องผู้ใช้งานจริง |
| **25** | `matplotlib.rcParams["font.family"] = name` | สั่งกำหนดให้ matplotlib ใช้ระบบฟอนต์นี้หลักทันที |
| **26** | `break` | ออกจากลูปการตรวจสอบฟอนต์ทันทีเมื่อเจอแล้ว |
| **27** | `matplotlib.rcParams["axes.unicode_minus"] = False` | หลีกเลี่ยงข้อผิดพลาดการแปลงตัวอักษรของสัญลักษณ์ลบในระบบแกนพล็อต |
| **30** | `_setup_thai_font()` | เรียกใช้งานฟังก์ชันตั้งค่าฟอนต์ทันทีเมื่อนำเข้าไฟล์นี้ |
| **33** | `def _path_edges(path):` | ประกาศฟังก์ชันจัดเตรียมลิสต์คู่เส้นเดินจากลิสต์ชื่อโหนด |
| **35** | `if not path:` | ตรวจว่าหากไม่มีเส้นทางส่งเข้ามา |
| **36** | `return []` | คืนลิสต์ว่างเปล่ากลับออกไป |
| **37** | `return list(zip(path, path[1:]))` | ซิปจับคู่ลิสต์โหนดที่อยู่ติดกัน (เช่น A, B, C -> [(A, B), (B, C)]) |
| **40** | `def draw_single(...)` | ประกาศฟังก์ชันจัดวาดผลแบบรูปเดี่ยวลงแกนพล็อต ax |
| **43** | `pos = get_positions(G)` | เรียกดึงพิกัดตำแหน่งของทุกโหนดบนกราฟ |
| **46** | `nx.draw_networkx_edges(...)` | วาดเฉพาะเส้นเชื่อมเปล่าสีเทาอ่อนและหนา 1.5 หน่วย |
| **47-48**| `nx.draw_networkx_nodes(...)` | วาดเฉพาะโหนดกราฟเป็นวงกลมสีเขียวอมฟ้าขนาด 600 หน่วยและขอบสีเข้ม |
| **49-50**| `nx.draw_networkx_labels(...)` | วาดตัวอักษรระบุชื่อกำกับตรงตำแหน่งโหนด |
| **53-54**| `edge_labels = { ... }` | คำนวณดิชันนารีจับคู่โหนดและตัวเลขน้ำหนัก (แสดงผลทศนิยม 1 ตำแหน่ง) |
| **55-56**| `nx.draw_networkx_edge_labels(...)` | สั่งเขียนตัวเลขน้ำหนักกำกับกึ่งกลางเส้นเชื่อมแต่ละเส้น |
| **59** | `if path:` | ตรวจว่าหากมีเป้าหมายเส้นทางในการระบายสี |
| **60** | `edges = _path_edges(path)` | จัดเรียงลิสต์เส้นทางออกเป็นคู่เส้นเชื่อม |
| **61-62**| `nx.draw_networkx_edges(...)` | วาดพ่นทับเส้นเชื่อมตามเส้นทางเดินด้วยสีสันและขนาดหนาพิเศษ 3.0 |
| **63-65**| `nx.draw_networkx_nodes(...)` | พ่นสีทับโหนดที่เป็นส่วนประกอบในเส้นทางนั้น ๆ เพื่อความสวยงามชัดเจน |
| **68** | `if start:` | ถ้ามีการระบุโหนดจุดเริ่มการเดินทางเข้ามา |
| **69-71**| `nx.draw_networkx_nodes(...)` | วาดพ่นสีทับโหนดเริ่มต้นเป็นสีเขียวเป็นกรณีพิเศษและมีขนาดวงใหญ่ขึ้น |
| **72** | `if goal:` | ถ้ามีการระบุโหนดปลายเป้าหมายเข้ามา |
| **73-75**| `nx.draw_networkx_nodes(...)` | วาดพ่นสีทับโหนดเป้าหมายปลายทางเป็นสีส้มเด่นเป็นกรณีพิเศษ |
| **77** | `ax.set_title(title, ...)` | กำหนดหัวเรื่องและลักษณะฟอนต์ของพล็อตย่อยนั้น |
| **78** | `ax.axis("off")` | ซ่อนขอบขีดและสเกลพล็อต x, y ออกเพื่อความโล่งสะอาดตา |
| **81** | `def draw_comparison(...)` | ประกาศฟังก์ชันสร้างแผนภาพเปรียบเทียบซ้ายขวาในหน้าต่างเดียว |
| **87** | `n = len(results)` | นับจำนวนของตารางแผนการทำงานที่ต้องการนำเสนอ |
| **88** | `fig, axes = plt.subplots(1, n, ...)` | สั่งเปิดหน้าต่างภาพ subplots แบบ 1 แถวและมีจำนวนคอลัมน์เท่ากับ n ภาพ |
| **89** | `if n == 1:` | ตรวจกันกรณีมีคอลัมน์เดียว |
| **90** | `axes = [axes]` | แปลงตัวแปรแกนเดี่ยวให้กลายเป็นลิสต์เพื่อรันลูปได้ |
| **92** | `for ax, res in zip(axes, results):` | วนลูปกระจายตัวแปรแกนรูป และ ข้อมูลผลลัพธ์ของแต่ละอัลกอริทึม |
| **93** | `title = res["name"]` | ดึงชื่อการทดลองขยายมาใช้ตั้งหัวเรื่อง |
| **94-95**| `if res.get("subtitle"): title += ...` | เอาซับไตเติลข้อมูลรายละเอียดมาพ่วงต่อท้าย |
| **96-97**| `draw_single(...)` | สั่งวาดเส้นทางและโหนดทับซ้อนลงไปบนพล็อตย่อยทีละอัน |
| **99-100**| `fig.suptitle(...)` | กำหนดหัวข้อหลักตัวโตอยู่ด้านบนสุดของหน้าต่างเปรียบเทียบ |
| **101** | `fig.tight_layout()` | คำนวณจัดระยะห่างระหว่างกรอบย่อยให้อัตโนมัติ |
| **103** | `if save_path:` | ตรวจว่าต้องการบันทึกเป็นไฟล์ลงระบบคอมพิวเตอร์หรือไม่ |
| **104** | `fig.savefig(save_path, ...)` | สั่งเซฟหน้าต่างแสดงผลเป็นไฟล์รูปภาพ PNG ปรับปรุงขอบให้ชิดกระชับ |
| **105** | `print(f"บันทึกรูปไปที่: {save_path}")` | แสดงข้อความรายงานผลการบันทึก |
| **106** | `return fig` | ส่งค่าออบเจกต์หน้าต่างรูปภาพกลับไป |
| **109-116**| `if __name__ == "__main__":` ... | บล็อกทดลองรันวาดกราฟเดี่ยวและเปิดภาพ matplotlib ขึ้นมาดูทันที |
